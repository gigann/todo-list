console.log('My code is running');

var todoListItems = [];

/**
 * Adds an item to the todo list.
 * @param {*} item
 */
var addItem = (item) => {
    todoListItems.push(item);
}

var populateHTML = () => {

}